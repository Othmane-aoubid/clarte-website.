{
  "services": {
    "frontend": {
      "root": "frontend",
      "framework": "nextjs",
      "buildCommand": "npm install --ignore-scripts && npm run build",
      "installCommand": "npm install --ignore-scripts"
    }
  }
}